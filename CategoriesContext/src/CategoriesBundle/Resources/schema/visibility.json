{
    "title": "visibility",
    "items": {
        "properties": {
            "op": {
                "required": true,
                "type": "string",
                "enum": ["replace"]
            },
            "path": {
                "required": true,
                "type": "string",
                "enum": ["/isVisible"]
            },
            "value": {
                "required": true,
                "type": "boolean",
                "enum": [true, false]
            }
        }
    }
}